{
  "name": "trivi-yeah-server",
  "version": "1.0.0",
  "description": "A socket based trivia app",
  "main": "index.js",
  "scripts": {
    "test": "mocha ./node_modules/.bin/mocha -c --reporter=spec --require test/setup.js --recursive \"test/**/*.test.js\"",
    "start": "nodemon -r dotenv/config app.js",
    "run_production": "node app.js"
  },
  "author": "Mel Delosada",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^4.0.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg-promise": "^10.5.6",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.1.2",
    "nodemon": "^2.0.4",
    "sinon": "^9.0.2"
  }
}
